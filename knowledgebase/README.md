# Knowledge Base

Put your economy and finance documents here as `.md` or `.txt` files, then run:

```bash
python scripts/ingest_knowledgebase.py --path knowledgebase
```

`README.md` is skipped by the ingestion script.
